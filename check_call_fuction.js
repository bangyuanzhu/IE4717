var nameNode = document.getElementById("new-useremail");
var pwdNode = document.getElementById("new-password");

nameNode.addEventListener("change", chkName, false);
pwdNode.addEventListener("change",chkpwd, false);